# TODO

## Transform

- [x] Ajouter `cbrt_inv` dans `pyperun/treatments/transform/run.py`
  Remplace `sqrt_inv` (B^(-1/2), monopole) par `cbrt_inv` (B^(-1/3), dipôle) comme défaut.
  `sqrt_inv` conservé dans `TRANSFORMS` pour rétrocompatibilité. Flows mis à jour. (2026-02-24)
